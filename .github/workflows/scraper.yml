name: NECMIS Daily Scraper

on:
  schedule:
    - cron: '0 11 * * *'
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - 'scraper.py'

jobs:
  scrape:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: Install dependencies
        run: pip install requests feedparser beautifulsoup4
      
      - name: Run NECMIS scraper
        run: python scraper.py
      
      - name: Commit and push changes
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add data/necmis_data.json
          git diff --staged --quiet || git commit -m "Update data - $(date -u '+%Y-%m-%d %H:%M UTC')"
          git push
```

5. Scroll down and click **"Commit changes"** (green button)

6. Click **"Commit changes"** again in the popup

---

**STEP B: Create the data folder the same way**

1. Click **"Add file"** → **"Create new file"**

2. Type this filename:
```
   data/necmis_data.json
```

3. **Copy and paste** the contents of `necmis_data.json` from your downloaded files
   - Open the file in Notepad (Windows) or TextEdit (Mac)
   - Select All (Ctrl+A or Cmd+A)
   - Copy (Ctrl+C or Cmd+C)
   - Paste into GitHub (Ctrl+V or Cmd+V)

4. Click **"Commit changes"**

---

### Your Final File Structure Should Be:
```
✓ .github/workflows/scraper.yml
✓ data/necmis_data.json
✓ index.html
✓ scraper.py
✓ README.md
